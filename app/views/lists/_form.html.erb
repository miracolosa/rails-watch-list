<%= simple_form_for(list) do |f| %>
  <%= f.input :name, label_html: { class: 'text-light' } %>
  <%= f.input :photo, as: :file, required: true, input_html: { required: true }, label_html: { class: 'text-light' } %>
  <%= f.submit class:"btn btn-outline-light" %>
<% end %>
